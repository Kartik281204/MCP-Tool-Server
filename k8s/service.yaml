apiVersion: v1
kind: Service
metadata:
  name: mcp-tool-server
  labels:
    app: mcp-tool-server
spec:
  selector:
    app: mcp-tool-server
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
